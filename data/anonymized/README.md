# Anonymized demo PDFs

These PDFs contain synthetic names, addresses, references, and decorative
non-scannable barcodes. They are safe examples for the PixoCrop interface and
documentation demo.

The private source documents are ignored by Git. Running `make demo-assets`
refreshes these copies when the private sources are available; otherwise, the
existing anonymized copies are reused to rebuild the WebP animation.
